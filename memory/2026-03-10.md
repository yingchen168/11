## Session durable notes (2026-03-10 UTC)
- 05:18 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 3/9 07:02 UTC 既有结论未见可验证新增；OKX 公告顶部仍为 Mar 6/Mar 5 既有条目（规则调整、OPN/ROBO、BTC/TRON 维护）；OKX Status 仍仅见 Mar 3 已完成维护；BNB 官方博客顶部仍为既有内容（含 2026.3.4 Beacon Chain Token Recovery Tool sunset）。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略；不作为失败条件。
  - Conclusion: `NO_REPLY`.
- 08:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: OKX Announcements 出现新帖（Mar 10: Wallet 将 Smart Account 更名为 Trader Mode），但不属于本任务推送口径（非上/下币、非充提暂停、非重大故障/风险公告）；其余来源未见可验证新增（OKX Status 仍为 Mar 3 已完成维护，BNB Blog 顶部仍为 3/4 既有内容）。
  - Source handling: BscScan 访问受限按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 08:25 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 08:24 UTC 未见符合推送口径的可验证新增；OKX 仅见同一条 Mar 10 钱包命名调整（非上/下币、非充提暂停、非重大故障/风险公告），OKX Status 与 BNB Blog 头部内容不变。
  - Source handling: BscScan 403 受限按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 09:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 发现可验证新增：OKX Announcements 顶部新增 “OKX to list perpetual futures for NG”（Published on Mar 10, 2026），属于上新（衍生品）事件；其余来源未见符合口径新增（OKX Status 仍为 Mar 3 已完成维护，BNB Blog 顶部未见重大故障/风险类新增）。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响总体结论。
  - Action: 已推送 1 条新增事件。
- 10:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 未见符合口径的可验证新增；OKX公告顶部仍为 Mar 10 的 NG 永续与钱包命名调整，其中 NG 永续已在 09:54 UTC 推送（24h 去重不重复推送）；OKX Status 仍仅见 Mar 3 已完成维护；BNB 官方博客顶部未见重大故障/风险类新增。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 11:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 未见符合推送口径的可验证新增；OKX公告仍以 Mar 10 的 NG 永续与钱包命名调整为最新，其中 NG 永续已于 09:54 UTC 推送（24h 去重不重复）；OKX Status 仍仅见 Mar 3 已完成维护；BNB 官方博客顶部仍为既有条目（含 2026.3.4 Beacon Chain Token Recovery Tool sunset），未见重大故障/风险类新增。
  - Source handling: BscScan 本轮访问受限按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 11:54 UTC cron `okx-bnb-onchain-watch`: rechecked OKX官方公告（latest/new listings）与BNB Chain官方Blog；未发现相较近24h去重窗口的可验证新增高优先级事件（上/下币、充提暂停、重大故障、风险公告、异常链上异动）。BscScan补充源访问受限已跳过。
- 11:54 UTC cron `okx-bnb-onchain-watch`: completed multi-source recheck via subagents (OKX help announcements/new listings/delistings/deposit-withdrawal + BNB official blog; BscScan supplementary). Only detected OKX NG perpetual listing (2026-03-10) which is already in 24h dedup state; no new pushable event this run.
- 12:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 未见符合推送口径的可验证新增；OKX公告最新仍为 Mar 10 的 NG 永续与钱包命名调整，其中 NG 永续已于 09:54 UTC 推送（24h 去重不重复）；OKX Status 仍仅见 Mar 3 已完成维护；BNB 官方博客顶部条目与前序轮次一致，未见重大故障/风险类新增。
  - Source handling: BscScan 访问受限按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 12:24 UTC cron `okx-bnb-onchain-watch` (rerun with parallel subagents):
  - Parallel subagents checked OKX Announcements, OKX Status, BNB Chain Blog, and BscScan supplementary.
  - Findings: no pushable newly-verified event versus 24h dedupe window. OKX still shows Mar 10 NG perpetual listing (already pushed at 09:54 UTC) and wallet rename note (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB blog latest remains prior dated posts (no <24h risk/fault anomaly item).
  - Source handling: BscScan inaccessible (403/challenge) treated as skipped supplementary source per stability policy.
  - Conclusion: `NO_REPLY`.
- 12:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 未见符合推送口径的可验证新增；OKX公告最新仍为 Mar 10 的 NG 永续与钱包命名调整，其中 NG 永续已于 09:54 UTC 推送（24h 去重不重复）；OKX Status 仍仅见 Mar 3 已完成维护；BNB 官方博客顶部仍为既有条目（含 2026.3.4 Beacon Chain Token Recovery Tool sunset），未见重大故障/风险类新增。
  - Source handling: BscScan 本轮访问受限（403）按单源失败策略忽略，不影响结论。
  - Conclusion: `NO_REPLY`.
- 12:55 UTC cron `okx-bnb-onchain-watch` (user-forced rerun with parallel subagents):
  - Parallel subagents checked OKX Announcements, OKX Status, BNB Chain Blog; supplementary BscScan checked separately.
  - Findings: no newly pushable verified event vs 24h dedupe window. OKX still has Mar 10 “OKX to list perpetual futures for NG” (already pushed at 09:54 UTC) and wallet rename note (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB Blog latest remains older posts (no <24h risk/fault anomaly item).
  - Source handling: BscScan inaccessible (403/challenge) treated as skipped supplementary source per stability policy.
  - Conclusion: `NO_REPLY`.
- 13:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（latest announcements）与 BNB Chain Blog；补充源 BscScan 本轮未作为必需判定。
  - Findings: OKX 仍显示 Mar 10 新帖“OKX to list perpetual futures for NG”与钱包命名调整；其中 NG 永续已于 09:54 UTC 推送，处于24h去重窗口；钱包命名调整不在推送口径。BNB Blog 顶部仍为既有条目（含 2026.3.4 Beacon Chain Token Recovery Tool sunset），未见符合口径新增。
  - Conclusion: `NO_REPLY`.
- 13:56 UTC cron `okx-bnb-onchain-watch` (forced completion rerun):
  - Parallel subagents completed checks for OKX公告、BNB官方Blog、BscScan补充源（受限可跳过）。
  - Findings:
    - OKX: 过去24h命中仅 `OKX to list perpetual futures for NG`（Mar 10, 2026），该事件已于 09:54 UTC 推送，24h去重窗口内不重复；未见新的下币/充提暂停/重大故障/风险公告。
    - BNB Chain Blog: 过去24h 无重大故障/风险/安全异常新增。
    - BscScan: 访问受限（Cloudflare/403），按补充源策略跳过，不影响结论。
  - Conclusion: `NO_REPLY`.
- 14:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary.
  - Findings: no newly verifiable pushable events. OKX still shows Mar 10 NG perpetual listing (already pushed at 09:54 UTC within 24h dedupe window) plus wallet rename notice (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB blog shows no new major incident/risk advisory matching push criteria.
  - Source handling: BscScan access restricted (403/challenge) skipped per stability policy; additional OKX category endpoints returned 404 and were ignored as non-fatal.
  - Conclusion: `NO_REPLY`.
- 15:20 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取（本轮成功拉取 3000 个 active/open 市场）；逐市场使用 `clob.polymarket.com/book?token_id=...` 获取盘口，单页/单book失败按规则跳过继续。
  - Filters applied:
    - 严格档：`2h < 到期 <= 72h`、互斥完备组合 `sum(min ask)<1`、按 1% 费滑保守扣减后净边际 `>=1.2%`、可成交深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、可成交深度 `>=5000`、价差 `<=0.01`。
  - Dedupe: 对比 `memory/polymarket-under1-near-expiry-state-v2.json`，仅新增或显著变化（概率>=1.5pct / 流动性>=20% / 方向翻转）推送。
  - Result: 本轮严格档 0、进取档 0；相较上轮无新增/显著变化，state-v2 已更新为空集，输出 `NO_REPLY`.
- 15:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX top items unchanged (Mar 10 NG perpetual listing already pushed at 09:54 UTC within 24h dedupe window; wallet Smart Account rename notice out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB blog shows no new major incident/risk advisory matching push criteria.
  - Source handling: BscScan access restricted (403/challenge) skipped per stability policy; single-source limitation did not affect overall result.
  - Conclusion: `NO_REPLY`.
- 15:26 UTC cron `okx-bnb-onchain-watch` (forced completion rerun):
  - Parallel subagents completed checks for OKX Announcements, OKX Status, BNB Chain Blog, and BscScan supplementary source.
  - Findings: no newly verifiable pushable events under criteria (listing/delisting, deposit-withdraw suspension, major outage, risk advisory, abnormal on-chain anomaly). OKX latest includes Mar 10 NG perpetual listing already pushed at 09:54 UTC (within 24h dedupe) and wallet rename notice (out of scope). OKX Status shows no active major incident/suspension; latest completed maintenance remains Mar 3. BNB Blog latest remains Mar 4 posts; no <24h major risk/security/outage advisory.
  - Source handling: BscScan access-limited (403/challenge) treated as skipped supplementary source per stability policy.
  - Conclusion: `NO_REPLY`.
- 15:50 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取（本轮成功拉取 4000 个 active/open 市场）；逐市场使用 `clob.polymarket.com/book?token_id=...` 获取盘口，单页/单book失败按规则跳过继续。
  - Filters applied:
    - 严格档：`2h < 到期 <= 72h`、互斥完备组合 `sum(min ask)<1`、按 1% 费滑保守扣减后净边际 `>=1.2%`、可成交深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、可成交深度 `>=5000`、价差 `<=0.01`。
  - Dedupe: 对比 `memory/polymarket-under1-near-expiry-state-v2.json`，仅新增或显著变化（概率>=1.5pct / 流动性>=20% / 方向翻转）推送。
  - Result: 本轮严格档 0、进取档 0；相较上轮无新增/显著变化，state-v2 已更新为空集，输出 `NO_REPLY`.
- 15:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing (already pushed at 09:54 UTC, within 24h dedupe window) and wallet rename notice (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB blog top remains prior Mar 4 posts with no new major incident/risk item.
  - Source handling: BscScan access-limited (403/challenge) skipped as supplementary source per stability policy.
  - Conclusion: `NO_REPLY`.
- 16:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX公告最新仍为 Mar 10 的 NG 永续（已于 09:54 UTC 推送，仍在24h去重窗口）与钱包命名调整（不在推送口径）；OKX Status 仍仅见 Mar 3 已完成维护；BNB Blog 最新为 3/10 与 3/4 的常规生态/AI内容，未见重大故障/风险公告。
  - Source handling: BscScan 403/Cloudflare challenge，按补充源策略跳过，不影响结论。
  - Conclusion: `NO_REPLY`.
- 16:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing (already pushed at 09:54 UTC, still within 24h dedupe window) and wallet rename notice (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB blog latest includes Mar 10 AI article + prior posts, no major outage/risk advisory matching push criteria.
  - Source handling: BscScan access-limited (403/challenge) skipped as supplementary source per stability policy.
  - Conclusion: `NO_REPLY`.
- 17:54 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX top items unchanged, with Mar 10 NG perpetual listing already pushed at 09:54 UTC (within 24h dedupe window) and wallet rename notice out of scope; OKX Status unchanged (latest completed maintenance Mar 3); BNB blog newest Mar 10 AI/agent-economy article is not a major outage/risk alert under push criteria.
  - Source handling: BscScan unavailable (access challenge/403) skipped per supplementary-source policy; single-source issue did not affect overall decision.
  - Conclusion: `NO_REPLY`.
- 18:24 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing (already pushed at 09:54 UTC, still within 24h dedupe window) and wallet rename notice (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB Blog latest includes Mar 10 AI/agent-economy article (not a major outage/risk advisory under this watch scope).
  - Source handling: BscScan access-limited (403/challenge) skipped as supplementary source per stability-first policy.
  - Conclusion: `NO_REPLY`.
- 19:27 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing (already pushed at 09:54 UTC, still within 24h dedupe window) and wallet rename notice (out of scope); OKX Status latest remains Mar 3 completed maintenance; BNB Blog top remains Mar 10 AI article + older ecosystem posts, no major outage/risk advisory matching push criteria.
  - Source handling: BscScan inaccessible (access challenge/403) skipped per supplementary-source policy; single-source limitation did not affect decision.
  - Conclusion: `NO_REPLY`.
- 19:29 UTC cron `okx-bnb-onchain-watch` (completion rerun with parallel subagents):
  - Parallel subagents checked OKX Announcements, OKX Status, BNB Chain Blog, and BscScan supplementary source.
  - Findings: no newly verifiable pushable events under criteria（上/下币、充提暂停、重大故障、风险公告、异常链上异动）. OKX latest still includes Mar 10 NG perpetual listing（已于 09:54 UTC 推送，24h 去重窗口内不重复）and wallet rename note（out of scope）; OKX Status latest remains Mar 3 completed maintenance; BNB Blog latest remains Mar 10 AI article + prior posts, none matching major outage/risk advisory.
  - Source handling: BscScan unavailable（access challenge/403）treated as skipped supplementary source per stability-first policy.
  - Conclusion: `NO_REPLY`.
- 19:57 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest unchanged: Mar 10 `OKX to list perpetual futures for NG`（already pushed at 09:54 UTC, still within 24h dedupe）and wallet rename notice（out of scope）; OKX Status latest remains Mar 3 completed maintenance; BNB Blog top remains Mar 10 AI article + prior ecosystem posts, no major outage/risk advisory matching criteria.
  - Source handling: BscScan blocked by access challenge（403）and skipped per supplementary-source policy.
  - Conclusion: `NO_REPLY`.
- 21:58 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing（already pushed at 09:54 UTC; still within 24h dedupe window）and wallet rename notice（out of scope）; OKX Status latest remains Mar 3 completed maintenance; BNB Blog latest includes Mar 10 AI article, no major outage/risk advisory matching criteria.
  - Source handling: BscScan access-limited（403/challenge）skipped per supplementary-source policy; single-source limitation did not affect result.
  - Conclusion: `NO_REPLY`.
- 22:00 UTC cron `okx-bnb-onchain-watch` (parallel subagents completion run):
  - Subagents checked OKX Announcements, OKX Status, BNB Chain Blog; BscScan supplementary checked separately.
  - Findings: OKX only matching item remains Mar 10 `OKX to list perpetual futures for NG` (already pushed at 09:54 UTC; within 24h dedupe window). OKX Status returned no qualifying <24h outage/suspension event. BNB Blog returned no qualifying risk/security/major-incident event in <24h.
  - Source handling: BscScan inaccessible (403/challenge), skipped per supplementary-source policy.
  - Conclusion: `NO_REPLY`.
- 22:58 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX latest remains Mar 10 NG perpetual listing（already pushed at 09:54 UTC; still within 24h dedupe window）and wallet rename note（out of scope）; OKX Status latest remains Mar 3 completed maintenance; BNB Blog latest remains AI/ecosystem posts with no major outage/risk advisory matching criteria.
  - Source handling: BscScan access-limited（403/challenge）skipped per supplementary-source policy; single-source limitation did not affect result.
  - Conclusion: `NO_REPLY`.
- 23:58 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements（`/help/category/announcements`）, OKX Status（`/status`）, BNB Chain Blog（`/en/blog`）; BscScan `contractsVerified` as supplementary source.
  - Findings: no newly verifiable pushable events. OKX top entries unchanged; only in-scope item remains Mar 10 `OKX to list perpetual futures for NG`（already pushed at 09:54 UTC, within 24h dedupe window）; wallet rename notice out of scope. OKX Status page still shows no qualifying new outage/suspension event. BNB Blog latest remains Mar 10 AI article + prior posts, no major incident/risk advisory in scope.
  - Source handling: BscScan blocked by access challenge（403）and skipped per supplementary-source policy.
  - Conclusion: `NO_REPLY`.
