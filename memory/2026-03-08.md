## Session durable notes (2026-03-08 UTC)
- 00:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements分类页、OKX Status（`/status`）、BNB Chain Blog；BscScan Tokens 作为补充源。
  - Findings: 相对 2026-03-07 23:34 UTC 结论无增量。OKX最新仍为 Mar 6 / Mar 5 既有公告（ROBO相关、规则调整、BTC/TRON维护）；OKX Status 未出现新的重大故障/维护；BNB官方来源未见可归类为“新合约/异常异动”的可验证新增。
  - Handling: 单源可用性或粒度不足按稳定性策略忽略，不影响总判断。
  - Conclusion: `NO_REPLY`.
- 00:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements分类页（可读到最新公告列表）、OKX Status（`/status`）、BNB Chain Blog；BscScan Tokens 作为补充源。
  - Findings: 相对 00:04 UTC 无可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告，Status 页仅见已完成维护项（Mar 3），BNB官方博客未见可归类“新合约/异常异动”的新增可执行事件。
  - Handling: 单源可用性/粒度不足按稳定性策略忽略，不影响整体结论。
  - Conclusion: `NO_REPLY`.
- 01:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources: OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog；BscScan Tokens 作为补充源。
  - Findings: 相对 00:34 UTC 无可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（ROBO相关、规则调整、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项，BNB官方博客未见可归类“新合约/异常异动”的可验证新增。
  - Handling: 单源异常（如部分OKX分类URL 404）按稳定性策略忽略，不影响总判断。
  - Conclusion: `NO_REPLY`.
- 02:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked sources (stability-first): OKX Announcements分类页、OKX Status（`/status`）、BNB Chain Blog、BscScan Tokens（补充源）；OKX `new-listings` 路径404按单源失败规则忽略。
  - Findings: 相对 01:04 UTC 未发现新增可验证重大动态；OKX最新仍为 Mar 6 / Mar 5 既有公告（ROBO相关、规则调整、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客未见可归类为“新合约/异常异动”的新增可执行事件。
  - Conclusion: 与上轮结论一致 -> `NO_REPLY`.
- 02:27 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取（单源失败容忍，失败即跳过不终止整轮）。
  - Applied filters:
    - 严格档：`2h < 到期 <= 72h`，事件级 Yes 组合 `sum(ask)<1`，并按保守扣减（费+滑点 1%）后净边际 `>=1.2%`，深度门槛 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`，政治/加密/体育关键词分类，主方向概率 `0.70~0.98`，流动性 `>=5000`，价差 `<=0.01`。
  - Dedupe: 与 `memory/polymarket-under1-near-expiry-state-v2.json` 对比，仅保留新增或显著变化（概率≥1.5pct / 流动性≥20% / 方向翻转）。
  - Result: 命中严格档 1 条、进取档多条变化，已更新 state-v2.
- 02:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对 02:04 UTC 未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（ROBO相关、规则调整、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（3/4等），无可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性/信息粒度不足按策略忽略，不影响整体判断。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 03:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对 02:34 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目，Status 仍仅见 Mar 3 已完成维护项；BNB官方博客未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性波动按策略忽略，不影响整体结论。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 04:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对 03:04 UTC 未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（规则调整、ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 补充源BscScan可访问但未形成异常异动可验证增量，按策略不单独触发推送。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 04:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对 04:04 UTC 未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（规则调整、ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（3/4等），无可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性/粒度不足按策略忽略，不影响整体判断。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 05:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对 04:34 UTC 未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（规则调整、ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（3/4 等），无可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性/粒度不足按策略忽略，不影响整体判断。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 07:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对上轮（06:34~07:05窗口既有结论）未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告，Status 仍仅见 Mar 3 已完成维护项；BNB官方博客未出现可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan作为补充源可访问但未形成异常异动可验证增量；任一单源异常均按策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 07:27 UTC cron `polymarket-under1-near-expiry`:
  - Data source: `gamma-api.polymarket.com/markets` paginated pull (~6000 active rows). Single-source tolerance preserved (source errors would be skipped without failing round).
  - Applied filters:
    - 严格档: `2h < expiry <= 72h`, event-level mutually-exclusive Yes组合 `sum(ask)<1`, conservative fee+slippage haircut 1% then net edge `>=1.2%`, executable depth gate `>=5000`.
    - 进取档: `2h < expiry <= 7d`, politics/crypto/sports keyword scope, dominant side prob `0.70<=p<0.98`, depth `>=5000`, spread `<=0.01`.
  - Dedupe/change rules: compared against `memory/polymarket-under1-near-expiry-state-v2.json` (new OR prob move>=1.5pct OR liquidity move>=20% OR direction flip).
  - Result: strict=0, aggr=0, incremental push=0 -> `NO_REPLY`.
- 08:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充核验端点（受限则跳过）。
  - Findings: 相对 07:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（ROBO相关、规则调整、BTC/TRON维护），Status 仅见 Mar 3 已完成维护；BNB官方博客未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan `contractsVerified` 本轮受限（403）已按单源失败策略忽略，不影响总体结论。
  - Conclusion: 与上轮一致 -> `NO_REPLY`.
- 08:57 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取；单源失败按策略跳过不中断整轮。
  - Filters applied:
    - 严格档：`2h < 到期 <= 72h`、互斥完备组合 `sum(min ask)<1`、费滑保守扣减后净边际 `>=1.2%`、深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、深度 `>=5000`、价差 `<=0.01`。
  - Dedupe baseline: 上轮 state 为 `memory/polymarket-under1-near-expiry-state-v2.json` 空集（08:27 UTC），本轮仅推送新增命中。
  - Result: 严格档 0；进取档命中 2 条（Castilla y León 区域选举相关）；state-v2 已更新。
- 09:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 08:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护；BNB官方博客顶部仍为 3/4 及更早条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan `contractsVerified` 本轮 403，已按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 13:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan Tokens 作为补充源。
  - Findings: 相对此前轮次未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性/信息粒度限制按策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 15:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 13:04 UTC 未发现可验证新增；OKX最新仍为 Mar 6 / Mar 5 既有公告（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 补充端点本轮受限（403）已按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 16:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 15:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，无可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan `contractsVerified` 本轮 403，按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.

- 17:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、BNB Chain Blog（`/en/blog`）；OKX其他公告路径异常按单源失败策略忽略；BscScan补充源受限则跳过。
  - Findings: 相对 16:04 UTC 未发现可验证新增；OKX公告顶部仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护）；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Conclusion: `NO_REPLY`.
- 18:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 17:34 UTC 未发现可验证新增；OKX公告顶部仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（含 3/4），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮受限（403）已按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 19:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 18:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（3/4等），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 补充端点本轮受限（403）已按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 19:57 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取（单页失败即跳过继续，不中断整轮）。
  - Applied filters:
    - 严格档：`2h < 到期 <= 72h`、事件级 Yes 组合 `sum(ask)<1`、费滑保守扣减 1% 后净边际 `>=1.2%`、深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、深度 `>=5000`、价差 `<=0.01`。
  - Dedupe: 对比 `memory/polymarket-under1-near-expiry-state-v2.json`，仅新增或显著变化（概率>=1.5pct / 流动性>=20% / 方向翻转）才推送。
  - Result: strict=0, aggr=0, incremental push=0 -> `NO_REPLY`.
- 20:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 19:34 UTC 未发现可验证新增；OKX公告顶部仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan `contractsVerified` 本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.

- 21:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 20:34 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（含 3/4），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 21:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 21:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（含 3/4），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 补充端点本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 22:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan 作为补充源（受限则跳过）。
  - Findings: 相对 21:34 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源可用性波动（含部分OKX路径404或补充源受限）均按策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 23:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 22:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（3/4等），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 补充端点本轮受限（403）已按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 23:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements分类页（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 23:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（含 3/4），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 补充端点本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
