## Session durable notes (2026-03-09 UTC)
- 02:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 2026-03-08 23:34 UTC 既有结论未见可验证新增；OKX公告顶部仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护；BNB官方博客顶部仍为既有条目（含 2026-03-04 Beacon Chain Token Recovery sunset），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮访问受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 03:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 02:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为既有条目（含 2026-03-04 Beacon Chain Token Recovery sunset），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 03:34 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan 作为补充源（可用性受限则跳过）。
  - Findings: 相对 03:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: 单源路径异常/可用性波动按策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 05:04 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 03:34 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目，未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 07:02 UTC cron `okx-bnb-onchain-watch`:
  - Rechecked prioritized sources (stability-first): OKX Announcements（`/help/category/announcements`）、OKX Status（`/status`）、BNB Chain Blog（`/en/blog`）；BscScan `contractsVerified` 作为补充源（受限则跳过）。
  - Findings: 相对 05:04 UTC 未发现可验证新增；OKX公告最新仍为 Mar 6 / Mar 5 既有条目（规则调整、OPN/ROBO相关、BTC/TRON维护），Status 仍仅见 Mar 3 已完成维护项；BNB官方博客顶部仍为 3/4 等既有条目（含 Beacon Chain Token Recovery Tool sunset），未见可归类“新合约/异常异动”的新增可执行事件。
  - Source handling: BscScan 本轮受限（403）按单源失败策略忽略，不影响总体判断。
  - Conclusion: `NO_REPLY`.
- 01:57 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取（单页失败即跳过继续，不中断整轮）。
  - Filters applied:
    - 严格档：`2h < 到期 <= 72h`、互斥完备组合 `sum(min ask)<1`、费滑保守扣减后净边际 `>=1.2%`、可成交深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、可成交深度 `>=5000`、价差 `<=0.01`。
  - Dedupe: 对比 `memory/polymarket-under1-near-expiry-state-v2.json`，仅新增或显著变化（概率>=1.5pct / 流动性>=20% / 方向翻转）推送。
  - Result: 严格档 0；进取档新增 1 条（DHS shutdown 30 days or more, Yes 97.8%）。state-v2 已更新。
- 03:27 UTC cron `polymarket-under1-near-expiry`:
  - Source: `gamma-api.polymarket.com/markets` 分页拉取；单页/单book失败按规则跳过继续，不中断整轮。
  - Filters applied:
    - 严格档：`2h < 到期 <= 72h`、互斥完备组合 `sum(min ask)<1`、费滑保守扣减后净边际 `>=1.2%`（按1%扣减）、可成交深度 `>=5000`。
    - 进取档：`2h < 到期 <= 7d`、政治/加密/体育、主方向概率 `0.70<=p<0.98`、可成交深度 `>=5000`、价差 `<=0.01`。
  - Dedupe: 对比 `memory/polymarket-under1-near-expiry-state-v2.json`，仅新增或显著变化（概率>=1.5pct / 流动性>=20% / 方向翻转）推送。
  - Result: 本轮无新增/显著变化信号，输出 `NO_REPLY`；state-v2 已更新。

## Durable notes (2026-03-09)
- User repeatedly emphasized reducing noise and duplicate pushes; requires strict dedupe and no repetitive alerts.
- Polymarket 'aggressive' filter tightened by user: keep original constraints + depth > $5000 and tight spread; later requested stricter practical handling for near-expiry/invalid alerts.
- User frustration with recurring failures; requested continuous improvement rather than repeated failure notifications.
- Operationally, cron prompts were repeatedly tightened for resilience and concise outputs.
